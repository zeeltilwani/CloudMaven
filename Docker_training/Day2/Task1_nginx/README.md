# Task 1: Nginx Docker Container

Created a custom Docker image using Nginx to serve a static HTML page.

Steps:
- Created index.html
- Wrote a Dockerfile using nginx base image
- Built Docker image using docker build
- Ran container using docker run
- Verified output on http://localhost:8080

Screenshots of build, container run, and browser output are attached.
